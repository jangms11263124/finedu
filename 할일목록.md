# 🎯 finedu 마무리 작업 — 할 일 목록 (2026-06-25 작성, 작업일 2026-06-26)

> **오늘의 목표**: 더미 데이터를 **실제 API 데이터로 연결**(P0)하고, 남은 수정사항을 마무리한다.
> 가장 중요한 작업은 **2번(실데이터 연결)** 이다. 1번(작업 방식)을 먼저 함께 읽고 시작할 것.

---

## 1. 작업 방식 (팀 공통 규칙) — 시작 전 함께 확인

### 1-1. 브랜치 / 커밋
- `master`에 직접 push 금지. 작업 단위로 브랜치 생성: `feature/contents-youtube`, `feature/events-api` 등.
- 커밋 메시지는 기존 컨벤션 유지: `[be] ...`, `[fe] ...`, `[ai] ...`
- 작업 시작 전 `git pull` → 충돌 줄이기 위해 **도메인 단위로 담당자 분리**(아래 역할 분담).

### 1-2. 외부 API 연결 표준 절차 (도메인마다 동일하게)
**백엔드 (Django)**
1. 모델에 필요한 필드가 있는지 확인 → 없으면 추가 후 `makemigrations` → `migrate`
2. 외부 API 호출 로직을 **management command** 또는 **서비스 함수**로 작성
   - 예: `python manage.py fetch_youtube_contents` (기존 `seed_demo.py` 구조 참고)
   - `httpx`는 이미 설치돼 있음(requirements). API 키는 **`.env`에서 로드**(`settings.py`의 `os.environ.get` 패턴 그대로).
3. 시리얼라이저/뷰는 대부분 그대로 재사용 가능(이미 REST 엔드포인트 존재). 필드만 매핑.
4. 적재 후 `/api/...` 엔드포인트로 실데이터가 내려오는지 확인.

**프론트엔드 (Vue)**
1. 이미 `api.get('/contents/')` 등으로 붙어 있으므로, **백엔드만 실데이터로 바뀌면 대부분 자동 반영**.
2. 응답 필드명이 바뀌면 해당 View의 바인딩만 수정.
3. **로딩 / 빈 상태 / 에러 상태** 세 가지를 반드시 처리(이미 패턴 있음 — `AIRecommendView.vue`의 스켈레톤·intro·error 참고).

### 1-3. 환경변수 / 키 관리 ⚠️
- 모든 API 키는 루트 `.env`에 넣고 **절대 커밋하지 않는다**(`.gitignore`에 `.env` 이미 등록됨).
- `.env.example`에 키 이름만(값 없이) 추가해 팀원이 무엇을 채워야 하는지 공유.
- 예: `YOUTUBE_API_KEY=`, `DATA_GO_KR_KEY=` 등.

### 1-4. 검증 방법
- **백엔드**: `python manage.py shell` 또는 `curl http://127.0.0.1:8000/api/<endpoint>/`로 실데이터 확인.
- **프론트**: `npm run dev`(5173) → 브라우저에서 페이지별 확인. 콘솔 에러 0 목표.
- 서버 2개 동시 구동 필요(Django 8000 + Vite 5173). CORS는 5173만 허용돼 있음.

### 1-5. 역할 분담 (제안)
| 담당 | 도메인 |
| --- | --- |
| A | 콘텐츠(YouTube) 백엔드 + 프론트 바인딩 |
| B | 교육 행사(공공데이터) 백엔드 + 프론트 바인딩 |
| C | 남은 수정(JWT refresh, AI 보강) + 금융상품 결정 |
| 공통 | 경제용어 점검, 배포 설정, 최종 QA |

---

## 2. 핵심 작업 — 실제 데이터 API 연결 (P0) 🔥

> 각 도메인은 **소스 확정 → 백엔드 적재 → 프론트 확인** 순서. ☐ 체크박스로 진행 관리.

### 2-1. 콘텐츠 (금융 교육 영상) — `contents` 앱
- **현황**: `seed_demo.py`가 임의 youtube_id로 더미 생성. 모델에 `youtube_id`, `thumbnail`, `category`, `views` 등 이미 존재.
- **소스 후보(확정 필요)**: YouTube Data API v3 (검색어: "금융 교육", "경제 상식" 등) — 영상 ID·제목·썸네일·설명 수집.
- ☐ YouTube API 키 발급 → `.env`에 `YOUTUBE_API_KEY=`
- ☐ `contents/management/commands/fetch_youtube_contents.py` 작성 (httpx로 검색 API 호출 → `Content` 저장, 카테고리 매핑)
- ☐ 썸네일은 `https://img.youtube.com/vi/{id}/mqdefault.jpg` 사용(별도 저장 불필요)
- ☐ 검증: `/api/contents/?recommended=1`, `/api/contents/?popular=1`에 실영상 노출
- **관련 파일**: `contents/models.py`, `contents/serializers.py`, `frontend/src/views/ContentsView.vue`, `ContentDetailView.vue`

### 2-2. 교육 행사 / 프로그램 — `contents` 앱 (Event)
- **현황**: `seed_demo.py` 더미. 모델에 `region`, `online_type`, `start_date~end_date`, `place_name`, `address`, `latitude/longitude`(카카오맵용) 존재.
- **소스 후보(확정 필요)**: 공공데이터포털(data.go.kr)의 교육/행사 API 또는 지자체 청년/금융 교육 프로그램 API.
- ☐ API 키 발급 → `.env`에 `DATA_GO_KR_KEY=`
- ☐ `contents/management/commands/fetch_events.py` 작성
- ☐ **좌표 주의**: 응답에 위/경도가 없으면 주소→좌표 지오코딩 필요(카카오 로컬 API). 좌표 없으면 지도 대신 "온라인" 처리되도록 기존 로직 활용.
- ☐ 검증: `/api/events/`에 실데이터, `EventDetailView`에서 지도/날짜/접수상태 정상
- **관련 파일**: `contents/models.py`(Event), `frontend/src/views/EventsView.vue`, `EventDetailView.vue`

### 2-3. 경제 용어 사전 — `glossary` 앱
- **현황**: 이미 `data/20260623_시사경제용어사전.xlsx` → `load_xlsx_terms` 명령으로 적재됨(준-실데이터). **새로 만들 필요 없을 수 있음.**
- ☐ 데이터 최신성/누락 점검만 수행. 필요 시 최신 xlsx로 교체 후 `python manage.py load_xlsx_terms` 재실행
- ☐ (선택) 공공 API(기획재정부 시사경제용어)로 자동화할지 팀 결정
- **관련 파일**: `glossary/management/commands/load_xlsx_terms.py`, `frontend/src/views/GlossaryView.vue`

### 2-4. 금융상품 — ⚠️ 결정 필요
- **현황**: `금융상품`은 **콘텐츠 카테고리(finance)로만 존재**. 전용 모델/페이지 없음(푸터에 "금융 상품" 링크만 있음).
- ☐ **팀 결정**: 금융상품 비교 기능을 별도로 만들 것인가?
  - 만든다면: 금융감독원 "금융상품 한눈에" API → 신규 앱/모델/뷰/프론트 페이지 필요(작업량 큼, 시간 고려).
  - 안 만든다면: 푸터 링크를 콘텐츠 필터(`/contents?category=finance`)로 연결하고 범위에서 제외.

### 2-5. 커뮤니티 — `community` 앱
- **현황**: 유저 생성 데이터. 데모 시드(`seed_demo`의 Post)만 더미.
- ☐ 실서비스 데이터로 전환: 데모 게시글 시드 제거 여부 결정(시연용으로 일부 남길지).
- ☐ 별도 외부 API 없음 — 사용자 작성 기능이 정상 동작하는지만 최종 점검.

---

## 3. 남은 수정 (P1)

- ☐ **JWT 자동 갱신**: 현재 access 토큰(2시간) 만료 시 refresh를 쓰지 않고 그냥 로그아웃됨.
  `frontend/src/api/index.js`의 401 인터셉터에서 `POST /api/accounts/token/refresh/`로 access 재발급하도록 구현. (백엔드 엔드포인트는 이미 존재)
- ☐ **AI 추천 규칙 기반 폴백 보강**(선택): GMS 장애 시 폴백 경로(`contents/ai.py`의 `_rule_recommend`)는 퀴즈 오답/출석 스트릭을 아직 반영 안 함. AI 경로에선 동작.
- ☐ **GMS 503 재시도**: GMS 게이트웨이가 간헐적 503 반환. `contents/ai.py`·`quiz/ai.py`의 httpx 호출에 짧은 재시도(backoff) 추가 고려.
- ☐ **데모 시드 정리**: 실데이터 적재 후 `seed_demo`가 실데이터를 덮어쓰지 않도록 운영/시연 절차 정리.

---

## 4. 배포 / 마무리 (P2)

- ☐ `settings.py`: `DEBUG=False`, `ALLOWED_HOSTS`, `CORS_ALLOWED_ORIGINS`를 배포 도메인으로 설정.
- ☐ **카카오맵 도메인 등록**: 배포 도메인을 카카오 개발자 콘솔의 사이트 도메인에 추가(미등록 시 지도 로드 실패).
- ☐ `.env` / 환경변수: `GMS_KEY`, `YOUTUBE_API_KEY`, `DATA_GO_KR_KEY`, 카카오 키 등 배포 환경에 주입.
- ☐ `README.md` 갱신: 현재 views 목록·앱 구조와 일부 불일치. 실행/시드/환경변수 안내 업데이트.
- ☐ 최종 QA: 회원가입→로그인→콘텐츠/행사/용어/퀴즈/추천/커뮤니티/마이페이지 전 플로우 점검(콘솔 에러 0).

---

## 5. 참고 — 직전 세션(2026-06-25)에 이미 끝낸 것

- ✅ 누락 마이그레이션 적용, 의존성 설치
- ✅ 지도 좌표 버그 수정(`seed_demo.py`) + 카카오맵 정상화
- ✅ GMS 키 연결 확인(Gemini + OpenAI 임베딩 모두 동작), 폴백 크래시 수정(print→logging)
- ✅ EBTI 결과 localStorage 누수 수정(`auth.js`)
- ✅ AI 추천: 진입 시 자동호출 → 버튼 클릭 시에만(스토어 캐시), 스켈레톤 UI
- ✅ 추천 신호: 지역 제거 + 퀴즈 오답/좋아요 본문/커뮤니티 확장/출석 스트릭 4종 추가
- ✅ 콘텐츠 페이지 AI 추천 배너, 마이페이지 "나의 활동 현황" 고도화

> 변경 파일(커밋 대상): `contents/ai.py`, `contents/views.py`, `quiz/ai.py`,
> `accounts/serializers.py`, `frontend/src/stores/auth.js`, `recommend.js`,
> `frontend/src/views/AIRecommendView.vue`, `ContentsView.vue`, `MyPageView.vue`, `.env`(미커밋)
